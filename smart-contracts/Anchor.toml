[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[programs.devnet]
clipchain = "CLIPCHAIN_PROGRAM_ID_REPLACE_ME"

[scripts]
test = "mocha -r ts-node/register tests/**/*.ts"

[toolchain]
solana_version = "1.18.11"
anchor_version = "0.31.1"
